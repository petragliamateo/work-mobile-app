import React from 'react';
import {Box, Text} from 'native-base';

const Navbar = () => {
  return (
    <Box px={5} py={2} bgColor="red.500">
      <Text fontSize={24}>Navbar</Text>
    </Box>
  );
};
export default Navbar;
